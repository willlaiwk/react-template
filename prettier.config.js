module.exports = {
  tabWidth: 2, // Specify the number of spaces per indentation-level.
  useTabs: false, // Indent lines with tabs instead of spaces.
  semi: true, // Print semicolons at the ends of statements.
  singleQuote: true, // Use single quotes instead of double quotes.
  trailingComma: 'all', // Print trailing commas wherever possible when multi-line.
  bracketSpacing: true, // Print spaces between brackets in object literals.
  jsxSingleQuote: false, // Use single quotes instead of double quotes in JSX.
  bracketSameLine: false, // Put the > of a multi-line JSX element at the end of the last line instead of being alone on the next line (does not apply to self closing elements).
  endOfLine: 'auto',
};
